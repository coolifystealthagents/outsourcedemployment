import {site,services,allBlogPosts,researchPosts} from '../data';export function GET(){const base=`https://${String(site.domain).toLowerCase()}`;const pages=Math.max(1,Math.ceil(allBlogPosts.length/20));const paths=['','/services','/blog','/blog/top-30-staffing-employment-outsourcing-companies','/research','/contact','/privacy','/terms','/cancellation-policy',...services.map(s=>`/services/${s.slug}`),...allBlogPosts.map(p=>`/blog/${p.slug}`),...researchPosts.map(p=>`/research/${p.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`)];const body=paths.map(x=>`<url><loc>${base}${x}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'content-type':'application/xml'}})}
