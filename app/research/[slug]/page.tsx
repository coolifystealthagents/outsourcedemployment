import {notFound} from 'next/navigation';
import {Header,Footer,CTA,JsonLd} from '../../components';
import {researchPosts,site} from '../../data';

export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)return {};const routeSpecific=p.slug==='philippines-candidate-scheduling-evidence-research';const openGraphModifiedDate=(p as {openGraphModifiedDate?:string}).openGraphModifiedDate;const openGraph=routeSpecific?{title:p.title,description:p.excerpt,url:`https://${String(site.domain).toLowerCase()}/research/${p.slug}`} : openGraphModifiedDate?{type:'article' as const,title:p.title,description:p.excerpt,url:`https://${String(site.domain).toLowerCase()}/research/${p.slug}`,modifiedTime:openGraphModifiedDate}:undefined;return {title:p.title,description:routeSpecific||openGraphModifiedDate?p.excerpt:undefined,openGraph,alternates:{canonical:`https://${site.domain}/research/${p.slug}`}}}
export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();const publishedDate=p.publishedDate;const modifiedDate=(p as {modifiedDate?:string}).modifiedDate??publishedDate;const articleUrl=`https://${site.domain}/research/${p.slug}`;const visibleDate=publishedDate?new Date(`${publishedDate}T00:00:00Z`).toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric',timeZone:'UTC'}):undefined;const handoff=(p as {handoff?:{href:string;label:string;body:string}}).handoff;const image=(p as {image?:string}).image;const topicalHandoff=p.slug==='philippines-candidate-scheduling-evidence-research'?'candidate-sourcing-coordination':undefined;return <><Header/><main className="section"><JsonLd data={{'@context':'https://schema.org','@type':'Article',headline:p.title,description:p.excerpt,url:articleUrl,...(publishedDate?{datePublished:publishedDate}:{}),...(modifiedDate?{dateModified:modifiedDate}:{}),mainEntityOfPage:articleUrl}}/><article className="container guide-article"><p className="eyebrow">Philippines staffing research{publishedDate&&<> · <time dateTime={publishedDate}>Published {visibleDate}</time></>}</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p>{image&&<img src={image} alt="Editorial illustration for this Philippines staffing research article" style={{width:'100%',height:'auto',borderRadius:'1rem',marginBottom:'1.5rem'}}/>}<div className="card">{p.body.map(x=><p key={x}>{x}</p>)}</div>{handoff&&<aside className="card" data-topical-handoff={topicalHandoff}><h2>Next step for this work</h2><p>{handoff.body}</p><a className="btn primary" href={handoff.href}>{handoff.label}</a></aside>}</article><CTA/></main><Footer/></>}
