import {notFound} from 'next/navigation';
import {Header,Footer,CTA,JsonLd} from '../../components';
import {researchPosts,site} from '../../data';

export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)return {};return {title:p.title,alternates:{canonical:`https://${site.domain}/research/${p.slug}`}}}
export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();const publishedDate=p.publishedDate;const articleUrl=`https://${site.domain}/research/${p.slug}`;const visibleDate=publishedDate?new Date(`${publishedDate}T00:00:00Z`).toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric',timeZone:'UTC'}):undefined;const handoff=(p as {handoff?:{href:string;label:string;body:string}}).handoff;return <><Header/><main className="section"><JsonLd data={{'@context':'https://schema.org','@type':'Article',headline:p.title,description:p.excerpt,url:articleUrl,...(publishedDate?{datePublished:publishedDate}:{}),mainEntityOfPage:articleUrl}}/><article className="container guide-article"><p className="eyebrow">Philippines staffing research{publishedDate&&<> · <time dateTime={publishedDate}>Published {visibleDate}</time></>}</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><div className="card">{p.body.map(x=><p key={x}>{x}</p>)}</div>{handoff&&<aside className="card"><h2>Next step for this work</h2><p>{handoff.body}</p><a className="btn primary" href={handoff.href}>{handoff.label}</a></aside>}</article><CTA/></main><Footer/></>}
